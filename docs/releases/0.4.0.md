---
doc_id: release_notes_0_4_0
doc_role: reference
doc_purpose: Astro 7 security upgrade, tag atmosphere and verified distribution record.
doc_scope: [release, package-upgrade, validation]
update_triggers: [release-change]
source_of_truth: false
depends_on: [CHANGELOG.md, UPGRADING.md, docs/releases/README.md]
---

# Release Notes: 0.4.0

## Changes and security

- Upgrade to Astro 7.3.2 and Sharp 0.35.4, addressing the AVIF image-processing advisory [GHSA-26w7-cxv4-gfx2](https://github.com/withastro/astro/security/advisories/GHSA-26w7-cxv4-gfx2). Update MDX, RSS, sitemap, checking/deployment tools and affected transitive dependencies.
- Require Astro `^7.3.2` in the theme peer dependency. This is a breaking compatibility change from the previous Astro 5/6 range.
- Preserve HTML whitespace with `compressHTML: true` and adapt the Playwright preview process lifecycle for Astro 7.
- Tag directories and tag archives share rain/dust with the blog list, retaining spotlight and flicker with softer ice-blue/lavender colors, slower sweeps and a static reduced-motion background.
- Reconcile documentation with implemented search, contents, tags, copy, image preview, routing, configuration and distribution boundaries.
- Make main and isolated-starter audits blocking in npm releases, including `--skip-checks`; starter sync audits before committing. GitHub Release creation and read-back are explicit completion requirements.

## Migration

Use the matching current starter in a new directory and migrate content/configuration following [UPGRADING](../../UPGRADING.md). Updating the npm theme alone does not upgrade Astro/Sharp, add the shared atmosphere component, or rewrite routes/configuration. Do not keep the old Astro 6 lockfile or bypass peer dependency errors.

```bash
npm create astro@latest -- --template anglefeint/astro-theme-anglefeint#starter
```

Existing 0.3.0 installations need deliberate dependency migration; publishing this release does not patch those projects automatically. The GitHub source archive is main source, not the generated starter.

## Validation and delivery

Published on 2026-09-15 (Asia/Shanghai):

- npm `@anglefeint/astro-theme@0.4.0`, with `latest` and exact-version download verified. Package source: `8e7b447c353a40279c91d270381f2f6854a51945`.
- Tarball SHA-1: `7846664617e0f82321bf6b7daed5b32f60873ffd`, matching the pre-publication package (106 files).
- Starter: `ddad8ea7a505b839ad6b1b0f95e35a5d3d54d909`, generated after maintainer fix `ff5c853ed789ea1604220dfb8017ec8acbbeb627`. The tag targets the npm source commit, not this later workflow fix or documentation closeout.
- The committed-source dry-run passed main checks, 47 unit/integration tests, zero Astro diagnostics, the independent starter audit/build matrix and npm packing. All 24 browser regressions and lint passed. Main built 106 pages and indexed 70 articles.
- Actual publication reused that unchanged source's functional checks with `--skip-checks`; main audit, isolated installation/CLI/audit, packing, authentication and post-publication download verification remained active. npm accepted the publish before registry availability; verification eventually succeeded without a second publish.
- The first starter sync was blocked before commit/push by four stale indirect dependencies in its old lockfile. The main-only maintainer fix refreshes compatible dependencies with `npm update --prefer-online`; the full retry passed audit/check/build and drift verification before pushing. No affected intermediate starter was delivered.
- A fresh project was created from the public remote-template command above. Its actual registry install used theme 0.4.0, Astro 7.3.2 and Sharp 0.35.4 without a workspace symlink. Independent audit reported zero known vulnerabilities.
- Remote-template acceptance passed English/Chinese post creation, repeat-run file preservation, custom-page creation, doctor/build, dev/preview HTTP routes, RSS, build-only sitemap/search manifest and all 24 browser regressions. The removed temporary image article returned 404. A test-harness assumption that sitemap exists in dev was corrected; sitemap is generated at build and was verified in preview. Temporary HTTP servers were stopped after acceptance.

GitHub closeout uses `v0.4.0` at the npm source commit and publishes the body of this record, with remote main/starter refs read back afterward. The documentation-only closeout commit is separate from the npm source.

Prior local security-upgrade validation also passed a benign AVIF decode/resize/PNG conversion with Sharp 0.35.4 and libheif 1.23.2. Results cover Windows, Node 24.14.1, npm, Chromium and static output; other operating systems, browsers and SSR deployments were not validated. Audit and functional checks are not exploit reproduction or a guarantee against unknown vulnerabilities.
