---
doc_id: release_notes_0_6_0
doc_role: reference
doc_purpose: Cinematic effect enhancements, About reading status placement and release evidence.
doc_scope: [release, package-upgrade, validation]
update_triggers: [release-change]
source_of_truth: false
depends_on: [CHANGELOG.md, UPGRADING.md, docs/releases/README.md]
---

# Release Notes: 0.6.0

## Changes

- Matrix: pale luminous rain heads and varied column speed/trail length, reusing the existing canvas and frame limits.
- Cyberpunk: blue/pink reflected light on card covers, stronger on hover/focus. Original rain, spotlight and flicker parameters are preserved.
- AI: up to six signal paths on the existing network graph, with arrival rings and no added canvas or animation loop.
- Hacker: a 360ms decorative title decode when opening a tool, with stable accessible names and localized grapheme handling. Closing/reopening cancels the previous timer. Reduced motion skips decoding and window-edge animation.
- About reading status: on screens at least 1360px wide, the toast sits 12px outside the content panel's right edge and 1rem above the viewport bottom. Narrow screens keep the corner position.
- Document lightweight, simple publishing and four vivid cinematic atmospheres as core product identity. The earlier experiment reducing rain/spotlights and changing home text/panel styling was reverted and is not included.

## Migration

Compatible with the current 0.5.x starter. The visual changes are package-owned; no configuration, route or content-schema migration is required. Install `@anglefeint/astro-theme@^0.6.0` explicitly: `npm update` within `^0.5.1` does not cross the 0.x minor-version boundary. Then check/build your site. The starter also replaces the About page's HTML escape regular expressions with equivalent literal `replaceAll` calls for compatibility with the new Astro language checker. New installations use:

```bash
npm create astro@latest -- --template anglefeint/astro-theme-anglefeint#starter
```

Starter synchronization is required to deliver the new package range/lockfile and managed documentation. GitHub source archives contain main source, not the generated starter template.

## Documentation review

Reviewed changes from `1fc9396` through this release preparation against actual scripts, CSS and browser checks. Updated AGENTS, its thin Claude/Cursor entry links, Visual Systems, the architecture map and upgrade guidance. README/config usage, schema, starter manifest and historical release notes need no behavioral rewrite: no new user-facing configuration or starter runtime file was added.

## Validation and delivery

- Published package source: `4a393cb0215455c1e561889cb47e92664da306eb`, pushed to main before upload. Package tarball SHA-1: `73e51e5d5e4da841396cf28078d0c23c7b9e45d0` (dry-run, real packing and the downloaded registry archive agree).
- Main validation: 50 unit tests, 156 Astro files with zero diagnostics, 106 built pages and five About runtime payload checks passed. Browser validation: 28 Playwright tests passed, including cinematic effects, reduced motion and About toast placement. Literal HTML escaping matched the former implementation across 261 input cases.
- Initial fresh-cache dry-runs were blocked by the new `@astrojs/language-server@2.17.0` / `@astrojs/astro2tsx@0.1.0` parsing the About escape regular expressions incorrectly; a cached installation with language-server 2.16.16 passed. The equivalent literal escaping fix was verified against the new parser and the installed starter matrix. No check was bypassed to publish.
- After the fix, both the full dry-run and real release gates passed: main audit and isolated installed-starter audit reported zero known vulnerabilities; isolated CLI/migration checks and all five build configurations passed. The fresh installation resolved Astro 7.3.3. No release skip flags were used.
- Registry delivery verified on 2026-09-18: `latest` resolves to `0.6.0`; the exact version downloads successfully and its SHA-1 matches the release archive. npm processing initially exceeded the 12-attempt verification window; verification was resumed after propagation without republishing the accepted version.
- Delivered starter: `ba61f4e098f70651eb060acfb8cbd91a39b0e2e6`, pushed to `origin/starter`, with theme range `^0.6.0`, matching lockfile and the About escaping fix. Starter audit reported zero known vulnerabilities; 42 Astro files had zero diagnostics and the 51-page build passed. The sync's return-to-main installation initially hit a Windows file lock from the local preview process; stopping that process, restoring dependencies and restarting preview resolved it. Managed-file synchronization check passed.
- Public-template acceptance passed from a fresh `npm create astro@latest -- site --template anglefeint/astro-theme-anglefeint#starter --yes --no-install --no-git` project: real installed package 0.6.0 (no workspace link), zero-vulnerability audit, new-post/new-page commands, duplicate-post preservation, doctor/check/build, 22 generated share images, dev/preview routes, RSS, language labels and live 1200×630 PNG responses. Preview sitemap and Pagefind metadata also passed. Temporary servers were stopped and the test project removed.
- GitHub closeout uses tag `v0.6.0` at the published package source above. The documentation-only closeout commit on main is separate from the npm source revision. Release: https://github.com/anglefeint/astro-theme-anglefeint/releases/tag/v0.6.0.

## Limitations

Desktop/mobile Chromium checks establish functional behavior, not low-end device GPU/frame-rate or battery performance. No new dependencies were added. The five initial effect files increased by approximately 1.3 KB when individually gzip-compressing their source; this is not a production bundle measurement and predates the About toast placement change.
