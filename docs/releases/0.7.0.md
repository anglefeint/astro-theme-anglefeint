---
doc_id: release_notes_0_7_0
doc_role: reference
doc_purpose: Optional music player configuration, migration and release evidence.
doc_scope: [release, package-upgrade, validation]
update_triggers: [release-change]
source_of_truth: false
depends_on: [CHANGELOG.md, UPGRADING.md, docs/releases/README.md]
---

# Release Notes: 0.7.0

## Changes

- Optional music player, off by default and hidden for empty playlists. No audio files or new dependencies are shipped.
- Independent native Audio core, session persistence and scoped presentation. Play creates the audio source; pause/resume reuses it. Track changes discard stale events and failed play promises cannot corrupt later playback.
- Luminous teal/blue glass panel, breathing LEDs (reduced-motion aware), progress seeking, previous/play/next, playlist, mute and volume controls. Device captions remain English; accessible names and playback feedback follow the five-language registry.
- Lower-left placement separates music from article controls. At widths up to 720px, pages start compact, reserve space for return-to-top, and hide that button only while music is expanded. Desktop collapse preference is remembered within the tab session.
- Configure `theme.music.enabled` and `tracks` in `src/site.config.ts`. Each track requires `title` and a public-path or HTTPS `src`; `artist` is optional. Public paths respect Astro's base URL.

## Migration

The matching starter schema, defaults and generated theme adapter are required to configure music. npm alone does not replace those files. Create a fresh starter and migrate content and personal settings as described in [UPGRADING](../../UPGRADING.md). No bundled demonstration music remains. A `^0.6.0` dependency range does not include 0.7.0.

```bash
npm create astro@latest -- --template anglefeint/astro-theme-anglefeint#starter
```

Navigation pauses playback; users click Play to resume the remembered track and position. This release does not add seamless cross-page playback or a router. GitHub source archives contain main source, not the generated starter template.

## Documentation review

Reviewed changes against main baseline `c700d40`: configuration → adapter → normalization → conditional ThemeFrame mount → component/controller/core/storage → emitted assets. Updated five-language READMEs, package README, architecture map, visual behavior and upgrade instructions. Historical release notes, routing/SEO contracts and agent/release process documents retain their existing meaning and were not rewritten. `suggest:docs` selected broad direct candidates; it reported no propagated-only candidates. The explicit music package export closes the installed-package resolution boundary.

## Validation and delivery

- Local preparation: 55 unit tests, 28 Chromium regression tests, 162 Astro files with zero diagnostics, 106-page build and five About runtime payload checks passed. Five locale home outputs contain no music mount; demonstration audio is absent from source and build output. Earlier interactive checks covered the enabled player on desktop and 390px/320px screens.
- Documentation follow-up corrected the stale claim in `anglefeint.md` that music was not implemented and recorded package/starter ownership in `docs/PACKAGING_WORKFLOW.md`.
- The local pre-commit automatic stash failed; a binary staged patch was saved and the same ESLint/Prettier lint-staged tasks passed with `--no-stash` before committing. Release gates remain enabled.
- The initial installed build check caught small music modules being inlined as data URLs rather than emitted files. Music asset imports now explicitly disable inlining so pages share cacheable resources. Publication remained blocked while the corrected installed build gate was rerun.

- Published npm source: `6964e87e9c87ec77358a84836747670830027af3`, pushed before publication. Version `0.7.0` and `latest` were verified by the release script, including downloading the exact archive after npm processing. Registry SHA-1 `485fe45d90d70025782c17726d9e770dc5955468` matches dry-run and real packing.
- Corrected source passed the full main check and the independent installed-starter audit/build gate: English/Chinese default locales × both prefix modes, optional discovery features disabled, and music enabled (six build scenarios). Enabled music output references real core/storage asset files. Main and isolated installations reported zero known vulnerabilities.
- Final dry-run and real upload used `--skip-checks` to avoid repeating the already-passing full main/build-matrix gates on the same runtime source. Both still ran the mandatory main and isolated-starter audits, independent installation, CLI/adapter checks, packing, and npm publish validation. No audit was skipped.
- Delivered starter: `8b503c7f5fd1017ba4827056a593129084594a1e`, with range `^0.7.0` and matching lockfile. Starter audit, 42-file Astro check (zero diagnostics), 51-page build and About runtime checks passed. Managed-file synchronization is clean; main dependencies were restored afterward.
- Public-template acceptance on Windows / Node 24.14.1 / npm used the exact public create-template command in a new temporary directory. Verified real installed package 0.7.0 without a workspace link, zero-vulnerability audit, new-post/new-page, duplicate-post preservation, doctor/check/build, 22 generated share images, dev/preview routes, RSS, language labels, sitemap/search metadata and live 1200×630 PNG responses. The music mount was absent and the deleted demonstration audio returned 404 in both dev and preview. Test servers stopped and the temporary project was removed after success.
- Tag `v0.7.0` targets the npm source revision above; later documentation closeout commits do not change the published source. GitHub Release: https://github.com/anglefeint/astro-theme-anglefeint/releases/tag/v0.7.0.

## Validation limits

This release's public-template acceptance used local Windows/npm, not a new Linux or pnpm matrix. Earlier enabled-player interaction checks and the Chromium suite do not establish Safari/Firefox or physical mobile-device behavior. Music remains opt-in and requires user-provided audio.
