---
doc_id: release_0_8_0
doc_role: reference
doc_purpose: Footer credits and demo configuration isolation release record.
doc_scope: [release, package-upgrade, starter-sync, validation]
update_triggers: [release-change]
source_of_truth: false
depends_on: [CHANGELOG.md, UPGRADING.md, docs/PACKAGE_RELEASE.md]
---

# 0.8.0 — Footer credits and demo identity

The shared footer displays `© <build year> <site title> · Theme by Anglefeint · Built with Astro`, with links to the theme repository and Astro. `theme.footer.showCredits: false` removes both technical credits; copyright and custom `site.tagline` remain. The new tagline default is empty; the former default `Built with Astro.` is treated as the built-in credit to avoid duplication. No all-rights-reserved notice or new browser script is added.

The demo has five-language homepage/About copy and its own identity. Starter sync and drift detection now use a neutral configuration source template for `src/site.config.ts`, also used by isolated installation checks. The demo domain/author/introduction are not distributed to starter users. About omits the email link when no email is configured.

## Migration

Use the matching starter schema/defaults and generated theme adapter to configure the credits switch. npm alone cannot replace them. A `^0.7.0` range does not include 0.8.0; follow [UPGRADING](../../UPGRADING.md), create a fresh template and migrate content and personal settings without replacing its helpers. Five-language User Guide 3 includes the configuration and behavior.

```bash
npm create astro@latest -- --template anglefeint/astro-theme-anglefeint#starter
```

GitHub source archives contain main/demo source, not the generated starter.

## Validation and delivery

Local verification passed: 56 unit tests, Astro diagnostics (zero errors/warnings), lint, documentation metadata, 30 Chromium tests, and the independent installed-starter build/audit matrix. Footer checks cover enabled/disabled links, retained custom tagline and desktop/mobile output in five locales. Neutral configuration mapping is checked during independent installation. The former email-only smoke assertion was updated to accept the demo's actual GitHub contact link.

- npm source: `30b4ababded7252adf2a1ece558f35e975e95490`. Published 0.8.0 with `latest` and exact-version tarball verified after registry propagation retries. Registry shasum `a1e0d74e17cd993a4e6ca50a107e3819c020af74` matches the local preparation package.
- Full `release:npm -- --dry-run` passed main checks and the independent installed build/audit matrix on that source. The real invocation used `--skip-checks` only to avoid repeating that completed matrix; it still repeated main and isolated dependency audits, installation/CLI checks and publication verification. All audits reported zero known vulnerabilities.
- Starter: `c8a836d1b384aeb742055464f5b0e5528a493e71`, pushed and read back. Checks/build/audit passed, and the neutral config mapping is in sync. After successful starter commit, restoring main dependencies initially failed because the agent's preview process held a Windows native module open; stopping that process and reinstalling restored the workspace. No repeated npm publication was performed.
- Cloudflare reported a successful build for the source commit. Live Chinese demo title/introduction/footer were verified, and all 70 articles across five locales plus pagination matched main. The Cloudflare dashboard's active deployment ID/settings were not independently read.
- [Remote Linux acceptance 35427632700](https://github.com/anglefeint/astro-theme-anglefeint/actions/runs/35427632700) passed both Node 22/npm and Node 24/pnpm jobs. Each installed public theme 0.8.0, executed all 14 starter script entries, passed audit and default/customized browser scenarios, including shown/hidden footer credits. The Node 22 main browser suite passed all 30 tests. Reports/screenshots were downloaded for verification; GitHub artifacts are retained for 14 days. Chromium only; no claim of Safari/Firefox or real Giscus service validation.
